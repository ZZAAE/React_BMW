const BtnCreate = () => {

    const handleCreate = () => {

    };

    return (
        <div>
            <button onClick={handleCreate}>작성 완료</button>
        </div>
    );
};

export default BtnCreate;